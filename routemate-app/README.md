# RouteMate portfolio demo

Original supplied React UI with a local demo API adapter. No keys, AI requests, real bookings or payments. Source app may differ from the current AI Studio app.

Build: npm ci, npm run lint, npm run build. Output: ../routemate-demo. Serve the portfolio over HTTP/HTTPS (ES module build).

Preview-only changes: src/demoApi.ts, API imports in App/CreateCourseScreen, persistent demo notice, compact device viewport.
